# V1 risk register

Scale: likelihood and impact are `Low`, `Medium`, `High`. “Trigger” is the observable condition that changes the plan. Owners are engineering roles until people are assigned.

| ID | Risk | L | I | Mitigation / contingency | Trigger and owner | Status |
|---|---|---:|---:|---|---|---|
| R01 | compiler bug exports unauthorized/private data | M | H | closed types/rules, pairwise/property/golden/sentinel tests, sole writer, human preview | any privacy metric/test >0; Privacy owner stops release | Open, zero tolerance |
| R02 | Action runtime/artifact gains private access | L | H | separate role/artifact, explicit denies, no tools, static/import and deployed IAM canaries | any unexpected allow/import; Security owner blocks deploy | Open, zero tolerance |
| R03 | model misses/false-links elevator pattern | M | M | bounded summaries, three-run 15-scenario eval, human candidate acceptance, tune prompt/input not policy | recall <0.90 or precision <0.95; Agent owner tunes or marks not ready | Open |
| R04 | AgentCore cold start/service/deployment threatens demo | M | H | stable CDK resource, preflight/warm harmless fixture, version rollback, recorded backup; Action stays isolated | p95 operation >30s or failed smoke; Platform owner evaluates superseding ADR fallback | Open |
| R05 | Nova 2 Lite quality insufficient for skeptical investigation/action | M | M | structured output, deterministic checks, prompt eval; model substitution only via ADR | quality target fails after two tuning cycles; Agent owner proposes ADR | Open |
| R06 | SES sandbox/domain/recipient not ready | M | H | verified sender/recipient/config set and mailbox smoke early; controlled address | preflight cannot send; Platform owner blocks live send/uses recorded backup | Open prerequisite |
| R07 | SES ambiguous outcome stalls demo | L | H | one call, `SEND_UNKNOWN`, config-event reconciliation, never retry, recorded backup | timeout/process death; Action owner quarantines/reconciles | Accepted behavior |
| R08 | revoke/send concurrency creates stale disclosure | L | H | final strong revalidation + transactional 60s fence; concurrency tests | race test failure/fence >60s; Privacy owner blocks | Open, zero tolerance |
| R09 | canonicalization/hash mismatch invalidates approval or hides mutation | L | H | one RFC 8785 module, golden vectors, backend sole hasher, constant-time verify | cross-platform golden mismatch; Domain owner blocks | Open |
| R10 | duplicate/forwarded evidence inflates corroboration | M | H | content root plus root-ID locator ancestry (ADR-017), bounded closure traversal, contributor+root recomputation, exact-canonical fact support groups, corpus tests | scenario 3/4 fails; Investigation owner blocks readiness | Open |
| R11 | logs/traces leak private prompts/data | M | H | allowlist logs, OTEL content processor, sentinel capture tests, short retention | sentinel/private key in telemetry; Security owner disables deploy/sending | Open, zero tolerance |
| R12 | demo bearer token/persona exposes private UI | M | M | high entropy/session storage/rotation/throttling/no public production claim | URL/token shared or hostile use; Platform owner rotate/reset | Accepted hackathon residual |
| R13 | reset deletes or misses non-DEMO/DEMO resources | L | H | exact namespace/confirmation, manifest, all-target validation, reset role/lock, no scan fallback | corrupt/missing manifest or non-demo target; Demo owner stops reset | Open |
| R14 | scheduler create/replay misses or duplicates verification | M | M | deterministic name/token/generation/event, GetSchedule reconcile, watcher CAS, DLQ/alarms | schedule/DLQ smoke fails; Commitment owner blocks watcher demo | Open |
| R15 | fixed logical timestamps cannot map to real Scheduler | L | M | audit logical/actual times; create future schedule; demo invokes same watcher contract | schedule rejects past/invalid time; Commitment owner fixes mapping | Mitigated by design |
| R16 | image parser/metadata leaks or is malicious | M | H | fixed checksum-only fixtures; Pillow pinned exactly at `12.3.0`; frozen accepted MIME set, `10_000_000`-byte, `16_000_000`-pixel and `8192`-dimension caps, single-frame rule, disabled truncated loading, bomb rejection, and total metadata discard; fixed review checklist bound to the source hash; arbitrary upload rejected ([ADR-018](../adr/ADR-018-safe-evidence-and-compile-commit.md)) | checksum/decode/review mismatch, or a Pillow advisory affecting the accepted decoders; Evidence owner excludes photo | Open |
| R17 | DynamoDB transactions exceed limits or item size | L | M | V1 caps, bounded per-group monitor apply, size tests, no metadata dumps | >80 transaction actions or >300KiB item; Persistence owner splits bounded operation | Open |
| R18 | AWS region/quota/CDK resource mismatch | M | H | `us-east-1`, account preflight, pinned CDK, synth/deploy early, explicit service quotas | clean deploy/smoke fails; Platform owner resolves before UI polish | Open prerequisite |
| R19 | shared access/API abuse causes cost/DoS | M | M | token, throttling, body/token limits, bounded agent operations, budgets/alarms | spend/requests exceed budget; Platform owner revoke token/disable API | Open |
| R20 | scope creep prevents vertical path | H | H | cut list, phase gates, one scenario/adapter/destination, ADR before changes | work outside current phase/core path; Principal engineer cuts immediately | Open |
| R21 | conservative proposal validator rejects good copy | M | L | the frozen grammar of [ADR-021](../adr/ADR-021-action-grounding-and-caveats.md) tells the model exactly which forms pass, so the ordinary path does not depend on luck; bounded re-proposal, visible bounded reason code, tune prompt or template; no bypass and no second-model adjudication | >20% valid drafts rejected in eval; Action owner refines the *specific* rule, never by adding a similarity threshold | Accepted trade-off |
| R22 | natural-language claim is cited but not truly entailed | M | H | universal citation on every claim, request, and caveat; exact match-equality token support for numbers, dates, and number words; substring support for names limited to cited safe text and the two safe labels; quotation and non-ISO dates rejected rather than grounded; skeptical agent; human exact preview; evaluation rating ([ADR-021](../adr/ADR-021-action-grounding-and-caveats.md)) | action support precision <1.0 fixed demo; Action owner blocks | Open |
| R29 | lifecycle progress is mistaken for disclosure authority | M | H | `CommunityCase` carries two counters; only case-owned authorization changes bump `authorization_version`, and the compiler snapshot, proposal validator, and send fence compare that counter while the fence separately checks case *state*; the exhaustive bump table is per-command and asserted by test in both directions ([ADR-020](../adr/ADR-020-case-authorization-version.md)) | any edge acquiring an authorization bump without a table row, or any fence comparing the OCC version; Privacy owner blocks | Open, zero tolerance |
| R30 | a read-only condition check becomes a write grant | L | H | the application holds `dynamodb:ConditionCheckItem` on `NS#*#VIEW_CURRENT#*` and no `PutItem`/`UpdateItem`/`DeleteItem` on either view prefix, so the compiler stays the sole creator of views by IAM rather than by convention; static negative-capability sweep of every synthesized allow, plus a post-deploy view-write canary ([ADR-022](../adr/ADR-022-action-draft-preview-and-transaction.md)) | any synthesized allow granting the application a view-prefix write; Security owner blocks deploy | Open, zero tolerance |
| R31 | an approved preview and the bytes actually sent diverge | L | H | two distinct fields with two owners — `ActionProposal.preview_hash` written at proposal time and `ActionExecution.rendered_message_hash` written by the sender — computed by one deterministic renderer over one canonical tuple including `from_identity_id`; a mismatch is a definite pre-send stale failure and never a "close enough" send ([ADR-022](../adr/ADR-022-action-draft-preview-and-transaction.md)) | any path that reuses one field for both, or renders from anything but the immutable proposal and its bound view; Action owner blocks | Open, zero tolerance |
| R32 | an approval digest stops verifying after a legal write, so integrity checks are deleted rather than fixed | L | H | the approval is fully immutable, `consumed_at` is removed and consumption is the execution reaching `SENDING`, and `hash_approval` omits only row bookkeeping, so recomputation is meaningful at any later instant ([ADR-023](../adr/ADR-023-approval-binding-and-immutability.md)) | any mutable field entering an authorization digest; Action owner blocks | Open |
| R33 | a documented sender write boundary is not expressible, so the sender can rewrite the proposal it is about to send | L | H | executions move to their own `NS#n#EXECUTION#a` Shareable partition, so `LeadingKeys` scopes the sender's `PutItem` to executions and denies the proposal, approval, pointer, view, and case prefixes; all Core access is denied outright; asserted by a negative-capability sweep and a post-deploy canary ([ADR-024](../adr/ADR-024-execution-partition-and-sender-boundary.md)) | any grant reaching an `ACTION#` partition from the sender role; Platform owner blocks | Open, zero tolerance |
| R34 | exactly-once email delivery is claimed where SES cannot support it | M | M | the stated property is at-most-one deliberate attempt per approved execution, guaranteed by the `APPROVED -> SENDING` compare-and-swap and the rule that no path calls SES from `SENDING` or a terminal state; `SEND_UNKNOWN` is quarantined with no retry route, and the residual — a delivered message recorded as unknown — is accepted rather than resolved by resending ([ADR-025](../adr/ADR-025-one-deliberate-ses-attempt.md)) | any document or code comment asserting exactly-once delivery, or any automatic retry from an ambiguous outcome; Action owner blocks | Open |
| R23 | manager reply spoof/malicious text creates bad commitment | M | M | authenticated transport before decode, SES receipt verdicts, `In-Reply-To` correlation to one immutable outbound locator, DMARC-verified sender digest equal to the correlated destination's, cited-span grounding, no agent persistence or resolution ([ADR-026](../adr/ADR-026-inbound-reply-trust-and-correlation.md), [ADR-027](../adr/ADR-027-commitment-extraction-grounding-and-authority.md)) | any accepted reply without an authenticated transport and a resolved locator; Commitment owner blocks | Open |
| R33 | Phase 11 never supplies an `InboundMailTransportAuthenticator`, so the reply path is provably unreachable in a deployed demo | M | M | Phase 9 ships the port, the attester, the verifier, a local authenticator gated to `local`/`development`, and a static test that the AWS composition passes `None`; the demo runs the fixture through the same boundary | Phase 11 planning without the authenticator named as a deliverable; Platform owner blocks the live reply step | Open |
| R34 | the outbound message locator is missing for an execution, so a genuine reply cannot correlate | L | M | the locator is a participant of the action case projection transaction rather than a later write, so it commits with the `ACTIONED` edge or not at all; `SEND_UNKNOWN` deliberately has none | a locator written outside that transaction, or a reply path that creates one; Action owner blocks | Open |
| R24 | documentation and code diverge | M | H | AGENTS source order, ADR-first architecture changes, doc/link/contract tests in CI | enum/hash/IAM/API diff; maintainer stops merge | Open |
| R25 | Investigator deflates contradiction materiality and a case reaches readiness a higher label would have blocked | M | M | deterministic terms still gate (2 independent sources, `SAME_ISSUE`, compilable purpose); affected facts still carry `CONTRADICTED` outward; Action must caveat them; mandatory human preview; inflation is fail-safe | an evaluation case labels a `MEDIUM`+ contradiction `LOW`; Investigation owner reviews the prompt, never the guard | Accepted residual (ADR-015) |
| R26 | fact-level `CORROBORATED` is rare because grouping is exact canonical equality | H | L | deliberate: case-level corroboration carries the authority and is unaffected; a similarity threshold would make weaker evidence look stronger | contributors expect a corroborated case to show corroborated facts; Investigation owner explains in UI copy, never by loosening grouping | Accepted trade-off (ADR-015) |
| R27 | a safe derivative and its authorization disagree across two stores | L | H | one content-addressed write before the transaction; the DynamoDB transaction is the sole commit point; an uncommitted object is unreferenced and confers no authority; unknown PUT resolved by `HEAD`, never by a second key ([ADR-018](../adr/ADR-018-safe-evidence-and-compile-commit.md)) | any current view referencing an absent object, or two keys for one derivative; Privacy owner stops release | Open, zero tolerance |
| R28 | a documented IAM boundary is not expressible in AWS | a written guarantee that does not hold | boundaries are asserted against the *synthesized* policy, not the intended one; the send fence has its own partition so `W(fence only)` is scoped by `LeadingKeys`; case-partition writes denied; negative-capability tests search every statement exhaustively ([ADR-019](../adr/ADR-019-send-fence-partition-isolation.md)) | any synthesized allow granting a Core write outside `NS#*#FENCE#*`; Security owner blocks deploy | Open, zero tolerance |

## Architecture-blocking questions

None remain. Operational values must be supplied before deployment: AWS account ID, chosen `us-east-1` account, SES verified sender/recipient, Bedrock/AgentCore quota/access, domain/base URL, budget, and demo token secret. Production identity, retention, arbitrary-upload scanning, and a real ambient adapter are deliberately deferred, not unresolved V1 choices.
